import { InvalidOnesPipe } from './invalid-ones.pipe';

describe('InvalidOnesPipe', () => {
  it('create an instance', () => {
    const pipe = new InvalidOnesPipe();
    expect(pipe).toBeTruthy();
  });
});
